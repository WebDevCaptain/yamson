[package]
name = "yamson"
version = "1.0.0"
description = "A simple tool to convert YAML to JSON and vice versa."
license = "MIT"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
